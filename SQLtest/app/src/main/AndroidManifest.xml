<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sqltest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
<!--        <activity android:name=".manager.AddStudent">-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity android:name=".MainActivity"></activity>-->
        <activity android:name=".Student"></activity>
        <activity android:name=".Teacher"></activity>
        <activity android:name=".Manager"></activity>

        <!--教师-->
        <activity android:name=".teacher.Addgrades"></activity>
        <activity android:name=".teacher.Updategrades"></activity>
        <activity android:name=".teacher.Querygrades"></activity>

        <!--管理员-->
        <activity android:name=".manager.Student"></activity>

        <activity android:name=".student.Querygrades_stu"></activity>

        <activity android:name=".tool.Grade.Querygrade_res"></activity>
        <activity android:name=".tool.Student.Querystudent_res"></activity>
    </application>

</manifest>